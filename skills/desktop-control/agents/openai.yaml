interface:
  display_name: "Desktop Control Fallback"
  short_description: "Legacy Windows GUI fallback"
  default_prompt: "Use $desktop-control-fallback only after official Computer Use is unavailable or insufficient."
policy:
  allow_implicit_invocation: false
