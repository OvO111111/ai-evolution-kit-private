interface:
  display_name: "PPTX Legacy Fallback"
  short_description: "Low-level PowerPoint repair fallback"
  default_prompt: "Use $pptx-legacy-fallback only after the official Presentations route is unavailable."
policy:
  allow_implicit_invocation: false
