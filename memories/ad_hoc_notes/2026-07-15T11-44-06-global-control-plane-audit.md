# Global control-plane audit correction

scope: Global Codex evolution and future full-system audits.

- A "comprehensive" self-audit is incomplete if it checks skills/plugins but omits the global instruction file, runtime config, automations, memory inputs, export projection, and fresh behavior tests.
- Keep global `AGENTS.md` compact and limited to durable user-specific boundaries. Skill workflows, update procedures, browser ladders, multi-agent protocols, and weekly sync rules belong in their owning skills, runtime instructions, playbooks, or automations.
- The compact baseline is 26 lines and covers direct Chinese communication, source-first execution, end-to-end safe autonomy, scoped project memory, safety boundaries, narrow skill selection, evidence tiers, and outcome-first reporting.
- `phase2_workspace_diff.md` is not present and must not be treated as a mandatory memory prerequisite. Use the current extension instructions and only source files that actually exist.
- Full audits must distinguish installed, routed, live-callable, and artifact-quality evidence; they must also verify that the sanitized export contains the same intended global behavior as the local machine.
